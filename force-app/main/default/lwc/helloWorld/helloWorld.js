import { LightningElement } from "lwc";

export default class HelloWorld extends LightningElement{
    // JS class names first letters should be Capital all others should be small
    // Pascal Case
}